# <改动主题>

> 状态：提案中 ｜ 执行中 ｜ 已闭环 ｜ 已否决 ｜ 搁置
> 场景分类：运行时/解释器 ｜ 变量与绑定 ｜ 生成器 ｜ 编译链 ｜ 配置裁剪 ｜ 集成/移植 ｜ 构建工具
> 提出者 / 日期：
> 关联：<关键代码路径、相关 doc、提交号>

---

## 1. 场景与动机

<在什么使用场景下暴露了什么问题或需求？不做这个改动会怎样？>

## 2. 现状

<当前行为是什么、关键实现在哪些文件/函数、已经有哪些实测数据。>

## 3. 方案

<打算怎么做。若有多个方案，列出对比与取舍，并说明最终选择及理由。>

## 4. 影响面

| 维度 | 影响 |
|---|---|
| 静态 RAM（`.data+.bss`） | |
| Flash（`.text+.rodata+.data`） | |
| 公共 API / ABI | |
| 配置开关（`scl_cfg.h`） | |
| 普通态 / mini 态 | |
| 生成物与工具链 | |
| 向后兼容性 | |

## 5. 执行步骤

1.
2.

## 6. 验证（必须可直接复制执行）

```powershell
# 例：
# python tools/scl_build.py test
# python tools/scl_build.py sizes
# arm-none-eabi-size <elf 或 .o>
```

期望结果 / 实测数据：

| 指标 | 改前 | 改后 |
|---|---|---|
| | | |

## 7. 闭环标准

- [ ] 编译：普通态 + mini 态零错误（`tools/scl_build.py check`）
- [ ] 回归：`tools/scl_build.py test` 全绿（含 `mini_boot_sim`）
- [ ] 体积：给出前后实测数字（注明优化级别与口径）
- [ ] 文档：结论已落到 `doc/arc` / `doc/debug` / `doc/spec`
- [ ] 提交：按 `add`/`fix`/`change`/`md` 分类提交并写清说明
- [ ] 回填：本文件补齐实测数据、提交号、遗留问题

## 8. 风险与回退

<风险点、触发条件、回退方式（改配置 / 回滚提交 / 保留旧路径）>

## 9. 闭环记录

| 日期 | 动作 | 结果 / 实测数据 | 提交 |
|---|---|---|---|
| | | | |
